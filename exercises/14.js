let arrayList =  ['a', 'b', 'c', 'd', 'e', 'f'];

// to empty out this array we can just set it equal to an empty array to create a new array
arrayList = [];

// can also set length to 0
arrayList.length = 0;

// splice method
arrayList.splice(0, arrayList.length);